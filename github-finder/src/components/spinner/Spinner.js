import React from 'react';
import Radar from './Radar.gif'
const Spinner = () => {
    return (
        <div>
            <img src={Radar} alt="loading..." />
        </div>
    );
}

export default Spinner;
